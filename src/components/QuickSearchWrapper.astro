---
// The modal in Layout.astro owns the clone list; this wrapper only mounts the
// button that opens it.
import QuickSearchButton from './QuickSearchButton';
---

<QuickSearchButton client:load />
